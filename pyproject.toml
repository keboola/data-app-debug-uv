[project]
name = "data-app-debug-uv"
version = "0.1.0"
description = "Minimal Streamlit app for debugging private PyPI / uv config in Keboola data apps"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.36",
]
